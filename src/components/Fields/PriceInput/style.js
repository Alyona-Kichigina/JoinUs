import styled from "styled-components"

export const StyleIcon = styled.div`
  font-size: 14px;
  font-weight: 700;
  display: flex;
  align-items: center;
  padding-right: 8px;
  color: var(--color-light-blue-2);
`
